{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm nx build web --skip-nx-cache && cd web && node ../scripts/generate-routes-manifest.js",
  "outputDirectory": "web/.next",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "cd web && next dev --port $PORT",
  "regions": ["iad1"]
}
