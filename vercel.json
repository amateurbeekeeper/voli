{
  "buildCommand": "pnpm nx build web --prod && node scripts/generate-routes-manifest.js",
  "outputDirectory": "web/.next",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "pnpm nx dev web",
  "regions": ["iad1"]
}
