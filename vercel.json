{
  "buildCommand": "pnpm nx build web --skip-nx-cache",
  "outputDirectory": "web",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "pnpm nx dev web",
  "regions": ["iad1"]
}
