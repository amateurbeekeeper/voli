{
  "buildCommand": "npx nx build web --prod && node scripts/generate-routes-manifest.js",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "pnpm nx dev web",
  "regions": ["iad1"]
}
